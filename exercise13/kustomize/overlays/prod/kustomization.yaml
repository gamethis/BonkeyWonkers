---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Build this overlay from the base. The path below does not resolve from
# this directory.
resources:
  - ../base

namePrefix: prod-

# TODO: scale the bonkey Deployment to 3 replicas (replicas override).
#
# TODO: pin the container image to bonkey-k8s:v1.0 (images override).
