# SPDX-License-Identifier: MIT-0
---
- hosts: localhost
  vars:
    images:
      - name: alertmanager
        container: prom/alertmanager:v0.24.0
  roles:
    - name: bonkey
      vars:
        bonkey_images: "{{ images }}"
